# layer/requirements.txt
# Dependencias del Lambda Layer — Motor GPA v2.4
# ─────────────────────────────────────────────────────────────────
# SAM empaqueta automáticamente este layer con:
#   sam build
#
# El boto3 de AWS runtime suele estar actualizado, pero se
# incluye explícitamente para garantizar versión mínima.
# ─────────────────────────────────────────────────────────────────

# AWS SDK — operaciones DynamoDB, S3, SQS, SNS, Cognito, Bedrock
# Rango acotado para builds reproducibles (evita un major incompatible).
boto3>=1.34.0,<2.0.0

# Render de PDF escaneado → imagen PNG para OCR (s3/ocr_extractor.py)
pymupdf>=1.24.0,<2.0.0

# NOTA: aws-lambda-powertools se retiró porque ningún módulo lo importa.
# Si se adopta (logger/tracer), volver a añadirlo aquí con rango acotado:
#   aws-lambda-powertools>=2.34.0,<3.0.0

# Decimal helpers para DynamoDB (evitar float → Decimal)
# (incluido en stdlib, no requiere instalación)
